# AI-Powered Resume Screening Tool

## Backend (Flask)
- Run `pip install -r requirements.txt` in the backend folder
- Start server: `python app.py`

## Frontend (React)
- Run `npm install` in the frontend folder
- Start app: `npm start`

## Usage
- Open http://localhost:3000 in your browser
- Backend runs at http://localhost:5000
- Upload resume (PDF/DOCX) and paste job description
- View match score and extracted info

## Features
- PDF/DOCX parsing
- NLP-based similarity scoring
- Simple, modern React UI

---
For demo/college submission. Extend as needed for production.
